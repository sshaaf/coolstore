<?xml version="1.0" encoding="UTF-8"?>
<suppressions xmlns="https://jeremylong.github.io/DependencyCheck/dependency-suppression.1.3.xsd">
    <!--
        This file contains suppressions for false positives or accepted risks
        in the OWASP Dependency Check security scanner.

        Add suppressions here when:
        1. A CVE is a false positive
        2. A vulnerability is accepted as a risk (document why)
        3. A vulnerability is mitigated through other controls
    -->

    <!-- Example suppression for a false positive -->
    <!--
    <suppress>
        <notes><![CDATA[
            False positive - this vulnerability doesn't apply to our usage
        ]]></notes>
        <packageUrl regex="true">^pkg:maven/com\.example/.*$</packageUrl>
        <cve>CVE-2023-XXXXX</cve>
    </suppress>
    -->

    <!-- Suppress vulnerabilities in test dependencies that don't affect production -->
    <!--
    <suppress>
        <notes><![CDATA[
            Test dependency - not included in production build
        ]]></notes>
        <packageUrl regex="true">^pkg:maven/org\.mockito/mockito\-core@.*$</packageUrl>
    </suppress>
    -->
</suppressions>
